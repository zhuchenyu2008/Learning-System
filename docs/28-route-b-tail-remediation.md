# 28-路线B收尾问题修整文档

## 1. 背景
路线 B 阶段验收已判定为“基本完成，阶段通过”，但仍有两个明确尾项：
1. notes 页面真实观看时长上报未闭环
2. 后端测试环境缺少 celery 依赖，导致阶段验收证据不完整

## 2. 修整目标
- 打通 notes 前端到后端的 `watch_seconds` 真实上报链路
- 补齐测试环境依赖并完成路线 B 验证补证
- 在不扩张业务范围的前提下，为路线 B 做更干净的收口

## 3. 范围
### B-tail-1
- 前端 notes library / note detail 实际阅读时长采集
- 调用 `GET /api/v1/notes/{note_id}?watch_seconds=...`
- 保持现有接口契约，不新增复杂埋点系统

### B-tail-2
- 补齐 celery 依赖环境
- 重跑后端关键测试
- 形成路线 B 更完整验收证据

## 4. 验收标准
- 前端 notes 页面存在真实 watch_seconds 上报逻辑
- 后端关键测试可执行
- 输出路线 B 收尾后的阶段结果
