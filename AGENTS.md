# AGENTS.md

## 项目目标

本项目是一个基于本地 Markdown 文件夹的学习系统，支持从录音、视频、图片、文本生成 Markdown 笔记，并进一步生成复习计划、知识点总结、思维导图和 Obsidian 兼容内容。

## 开发原则

1. 每次只实现一个垂直功能切片。
2. 修改数据库结构前，必须先检查 `docs/03-data-model.md`。
3. 修改 AI/RAG 逻辑前，必须先检查 `docs/05-ai-rag.md`。
4. 修改权限逻辑前，必须先检查 `docs/08-security-rbac.md`。
5. 修改前端页面前，必须先检查 `docs/pages/` 下对应页面文档。
6. 每次修改后必须运行 lint、typecheck、test。
7. 每次完成后必须总结：
   - 修改了哪些文件
   - 实现了什么
   - 有哪些风险
   - 下一步建议

## 禁止事项

1. 不要绕过现有架构重新设计。
3. 不要把用户 API Key 写死。
4. 不要直接破坏用户原始 Markdown 文件，包括文档（/docs）。
5. 不要在没有测试的情况下修改核心文件处理逻辑。

## 推荐开发顺序

1. 项目脚手架
2. 本地文件夹配置
3. Markdown 文件扫描
4. 笔记库页面
5. Markdown 渲染
6. AI 配置
7. 笔记生成
8. 复习任务
9. RAG 检索
10. Obsidian 导出/同步
11. RBAC 权限
12. Docker 部署