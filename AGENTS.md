# AGENTS.md

本文件是 Learning-System 仓库的协作指南，适用于整个仓库。后续 AI agent 或工程协作者在修改本仓库前，应先阅读本文件和相关设计文档，并保持变更与现有产品方向一致。

## 项目定位

Learning-System 是一个面向课堂学习资料沉淀的 Web 系统，目标是把录音、视频、图片、PPT、PDF、文本等资料转化为可复习、可检索、可导出的学习知识库。

核心闭环：

```text
上传资料
  -> 解析资料
  -> 生成 Markdown 笔记
  -> 生成复习卡片
  -> FSRS 复习调度
  -> RAG 问答
  -> 知识点总结
  -> 思维导图
  -> Obsidian Markdown 导出
```

## 主要资料入口

修改前优先阅读与任务相关的文档：

- `docs/README.md`：文档索引与全局约定。
- `design.md`：完整产品、技术、数据流和验收设计。
- `stitch-frontend-design.md`：前端视觉与交互设计约定。
- `docs/00-product-requirements.md` 至 `docs/10-testing-acceptance.md`：核心设计文档。
- `docs/pages/*.md`：具体页面设计文档。

如果文档之间出现冲突，优先保持核心原则一致，并在修改中同步修正相关引用或说明，避免只改一处造成后续理解偏差。

## 工作约定

- 使用中文 Markdown 编写项目文档，保持术语、页面名称、角色名称和信息架构一致。
- 不提交真实 API key、密钥、token、账号密码、私有路径或生产配置。
- 数据库是唯一主存储；Obsidian 文件夹只作为自动导出的阅读副本，不作为业务数据源。
- 上传源文件不得进入 Obsidian 自动导出目录。
- 复习调度使用 FSRS，不引入 SM-2 作为默认算法。
- 长耗时任务必须设计为异步队列流程；Web 请求只负责创建任务和查询状态，不同步等待任务完成。
- 普通用户与管理员共享同一套界面，但普通用户不可用的管理能力应前端禁用并由后端强校验。
- 普通用户不能上传、生成、编辑、删除、配置系统、导入导出数据或查看其他用户日志。
- 管理员负责上传资料、触发生成、编辑内容、配置 AI 与 Obsidian、管理用户、查看任务和系统日志。

## 后续实现默认方向

如果用户明确要求从文档进入代码实现，默认遵循以下技术方向，除非相关文档或用户请求另有说明：

- Web：Next.js App Router、React、TypeScript。
- 样式：Tailwind CSS 或 CSS Modules，配合自定义织物质感设计 token。
- 数据库：PostgreSQL。
- 向量检索：pgvector。
- ORM：Drizzle ORM。
- 异步任务：BullMQ + Redis。
- 文件存储：本地 Docker volume 起步，并保留可替换对象存储抽象。
- AI 调用：OpenAI 兼容 HTTP 客户端，保留 provider adapter。
- Markdown 渲染：`react-markdown`、`remark-gfm`、受控 `rehype-raw`、KaTeX、Mermaid、Markmap。
- 测试：Vitest、Playwright。
- 部署：Docker Compose。

实现阶段应保持 Web、Worker、Scheduler 的职责边界。MVP 可以把 Scheduler 合并到 Worker 进程，但文档和代码结构仍应体现独立职责。

## 前端与交互约定

- 构建真实应用工作区，不做营销落地页。
- 桌面端采用固定左侧树状导航和单一主工作区。
- 主工作区可以使用顶部状态带、标签页、抽屉、底部详情区或折叠区，但不做固定左中右三栏工作台。
- 移动端使用顶部模块切换、抽屉导航或底部一级导航补充入口，页面主体保持单列。
- 视觉风格保持安静、可读、带轻微织物质感：浅亚麻背景、画布式工作面、缝线分隔、克制中性色和少量墨蓝/苔绿/赭色状态点缀。
- UI 圆角通常控制在 6-8px，卡片圆角不超过 8px。
- 避免卡片套卡片，避免把页面大区块都做成浮动卡片。
- 禁用态能力应可见、可理解，并提示需要管理员权限。
- 动效保持短而轻，通常控制在 180-260ms。
- 文本可读性优先，纹理不得影响对比度。

## 安全与权限

- 所有写接口都必须校验登录状态、角色权限和输入 schema。
- 后端必须拒绝越权请求，不能只依赖前端禁用态。
- 密码使用强哈希，session token 只保存哈希。
- API key 加密存储，前端只展示尾号或脱敏信息。
- 日志、任务错误、导出包、页面源码和测试快照不得包含明文 API key。
- 关键写操作需要记录审计日志，包括登录成功/失败、用户与角色变更、内容创建/编辑/删除、AI 配置变更、Obsidian 配置变更、导入导出、任务重试和取消。

## 文档修改检查

文档变更完成后，至少检查：

- Markdown 标题层级、列表、代码块和链接渲染正常。
- 中文显示正常，没有乱码或中英术语混乱。
- 文档仍明确数据库是唯一主存储。
- 文档仍明确 Obsidian 是导出目标，不是业务数据源。
- 文档仍明确使用 FSRS，不使用 SM-2。
- 文档仍明确异步任务架构。
- 文档仍明确普通用户与管理员权限边界。
- 相关页面文档、核心文档和索引之间的链接与说法一致。

## 仓库卫生

- 不要在第一轮文档阶段新增应用源码目录、数据库迁移、Docker Compose 文件或依赖锁文件。
- 不要提交本地缓存、构建产物、临时文件、日志文件或私有配置。
- 若需要新增示例配置，只能提交脱敏模板，并明确真实值由管理员在设置页或部署环境中配置。
- 不允许任何未经允许的文档删减，添加已有文档时保持范围聚焦，不做无关重写。
